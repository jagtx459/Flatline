{
  "name": "flatline",
  "version": "0.1.0",
  "description": "Monitor ICMP/HTTP endpoints, and arms a script for graceful shutdown or migrate infrastructure after a configurable wait period.",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "nodemailer": "^9.0.3",
    "ssh2": "^1.17.0",
    "yaml": "^2.9.0"
  }
}